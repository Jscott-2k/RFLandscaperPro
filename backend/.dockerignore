# Node artifacts
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist
coverage
.tmp
.cache

# Env & secrets
.env
.env.*
!.env.example

# VCS & local tooling
.git
.gitignore
.gitattributes
.vscode
.idea

# Docker & compose files not needed in context
docker-compose*.yml
docker-compose*.yaml

# OS cruft
.DS_Store
Thumbs.db

# Keep lock & package manifests (needed for npm ci)
!package.json
!package-lock.json